# Copy this file to .dev.vars for local `wrangler dev` runs (never commit .dev.vars).
# In production, set the real value with: wrangler secret put JWT_SECRET
JWT_SECRET=troque_por_uma_chave_longa_e_aleatoria
