{
  "name": "portfolio-api",
  "version": "2.0.0",
  "description": "API REST para portfolio - Alex Cigoli (Cloudflare Workers + Hono + D1)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "hash-password": "node scripts/hash-password.mjs",
    "db:migrations:apply:local": "wrangler d1 migrations apply portfolio-db --local",
    "db:migrations:apply:remote": "wrangler d1 migrations apply portfolio-db --remote"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
